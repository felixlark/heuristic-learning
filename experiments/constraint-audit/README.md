# Constraint Audit Experiment Records

Generate the tracked report with:

```bash
npm run examples:constraint-audit:feedback
```

The report records a closed-world constraint probe only. It does not evaluate a live model, retrieval system, answer quality, or general hallucination rate.
